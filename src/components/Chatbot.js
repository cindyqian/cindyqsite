// Chatbot disabled: was importing Node `os` and using invalid env access in the browser.
// To restore: uncomment import + `/Chatbot` route in App.js and the NavBar link;
// wire API keys via CRA env (REACT_APP_*) or a backend proxy instead of `os`.

export default function Chatbot() {
  return null;
}
